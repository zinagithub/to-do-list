import './style.css';
import initPage from './init-page.js';

var categories = [];
initPage(categories);
//initCategories();

if (categories.length==0){
	//alert("c'est vide")
}else{
	//renderCategories()
}