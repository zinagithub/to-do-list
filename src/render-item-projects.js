/* 1 - import data from data.js 
   2 - filter and display all project with catname categorie 
   3 - add new and delete project buttons if they don't exist
   4 - listen the button */
   
const renderItemProjects = function (catName){
	
	const projectCat = document.getElementById("body-item");
	
	
}
export default renderItemProjects;